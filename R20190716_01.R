# 1일차 수업 - 20190716(화)

# 패키지 설치/불러오기(부착)/확인/제거(해제)
install.packages("ggplot2")
library(ggplot2) # 부착

detach("package:ggplot2") # 해제
search() # 패키지 확인
installed.packages() # 전체 패키지 확인
update.packages()

### 변수 : 값을 저장하는 공간

a = 10
b <- 20
